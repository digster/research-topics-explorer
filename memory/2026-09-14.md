# 2026-09-14

## Task: add a Critical Theory topic to `research-topics.csv`

> add a new topic for critical theory in the research-topics.csv file.

Data-only change (plus the mandatory `data.js` regeneration). No app or parser
code touched.

### Placement decision
There was **no** Critical Theory / Frankfurt School topic in the dataset — the
nearest neighbours were `v5-media-theory-and-philosophy-of-technology`,
`v9-michel-foucault`, and `v9-actor-network-theory-and-sts`. Filed the new row
into the **existing latest cohort** rather than opening a `v10`:

- id: `v9-critical-theory-and-the-frankfurt-school`
- version **v9**, group **A — Disciplines & Fields**, `id_code` **A7** (next free)
- discipline **5 — Culture, History & Worldview** (with Foucault / ANT / Sociology)
- `likely_phase` **5**, matching the other discipline-5 Group A rows

Rationale for not minting `v10`: the pipeline is version-agnostic so a new tag
costs no code, but it *would* add a new accent colour, stats-bar entry, graph
legend chip, and filter checkbox for a single row. Worth it for a batch, not
for one topic.

### Row contents
Followed the **v9 Group A field dialect** (copied shape from `A1`/`A4`, not from
an arbitrary row): `tier`, `phase`, `phase_rationale`, `primary_source` all
empty — `tier` is safe empty because `parse.mjs` falls back to `group_code`
("A"). 7 key figures: Adorno, Horkheimer, Benjamin, Marcuse, Habermas, Gramsci,
Fraser. 10 connections (Media Theory, Foucault, Political Philosophy,
Semiotics, Aesthetics, Attention Economy, Sociology, ANT/STS, History &
Philosophy of Science, Epistemology) — **all 10 resolved to real ids**, zero
unresolved slots, so no faded chips.

Initially shipped **without** reciprocal links (flagged to the user as
`in 0 · out 10`); the user then established that **reciprocal links should
always be updated when required**, and they were added — see the follow-up
section below.

### The gotcha worth remembering (→ new `LEARNINGS.md`)
The CSV has a **CRLF header** but LF-terminated records. Splitting the header
on `,` gave a final column named `"source_date\r"`, so a row object keyed by
the clean name didn't match. Caught only because the append script asserted
"header column set == row key set" before serializing.

**This would not have been caught downstream**: `parse.mjs` strips `\r` in its
character loop, so a misaligned row parses *cleanly* and emits a wrong
`data.js` with **zero warnings**. Appended with `\n` to match the surrounding
records and keep the diff a one-line addition.

Created `LEARNINGS.md` (didn't exist) documenting this plus: CSV edits are
always a two-file change; `connects_to_*` positional alignment (a *shorter*
ids list fails silently); per-cohort field dialects; a version tag is a UI
facet; shared filter state spans all views.

### Verification
- `node parse.mjs` → **204 topics** (v5=56, v6=32, v7=66, v8=18, **v9=32**),
  **844 edges** (was 834), 263 unresolved slots (unchanged), **no `⚠` warnings**.
- `node --test parse.test.mjs` → **20/20**, incl. the integration test that
  asserts zero warnings on the real CSV.
- In-browser (`python3 -m http.server 8000`):
  - Catalog search "critical theory" → card renders: v9 badge, "Culture,
    History & Worldview" tag, figures line, lane *Foundations*, **10 links**.
  - Detail panel: `in 0 · out 10`, full description, **10 resolved chips**
    (none faded).
  - Knowledge Graph: 204 nodes / 844 links; new node present with **10 edges**
    to exactly the intended targets.
  - Thinkers: **all 7** figures survive `looksLikePerson()` and are indexed.
  - Deep link `#view=catalog&topic=v9-critical-theory-…` opens the panel on load.
  - **No console errors.**
  - Note: the Catalog search filter narrows *every* view (shared
    `isTopicVisible`) — the graph showed 2 nodes until I cleared it. Not a bug.
- Thinkers index: people **794 → 801**; featured (2+ topics) stays **48** — none
  of the 7 names appears in any other topic, confirming a genuine gap rather
  than duplicate coverage.

### Docs touched
README (3 stale counts: 203→204, parser sample output incl. edges 834→844 and
v9=31→32, "v9 batch: 31 topics"→32), ARCHITECTURE (`~794`→`~801` people; no
architectural change otherwise), new LEARNINGS.md, PROMPT, this memory.

### Suggested commit message (superseded — see the combined message below)
```
data: add Critical Theory & the Frankfurt School topic

The dataset had no Critical Theory entry despite carrying Foucault, ANT/STS,
and Media Theory — the discipline those topics implicitly lean on.

Add v9-critical-theory-and-the-frankfurt-school as id_code A7 in the existing
v9 "Group A — Disciplines & Fields" cohort, under discipline 5 (Culture,
History & Worldview) with likely_phase 5. Filed into v9 rather than a new v10
tag: a version tag is a UI facet (accent colour, legend chip, filter checkbox,
stats entry) and is not worth minting for a single row.

The row follows the v9 Group A dialect (tier/phase/phase_rationale/
primary_source empty, placement via likely_phase) and wires 10 connections —
Media Theory, Foucault, Political Philosophy, Semiotics, Aesthetics, Attention
Economy, Sociology, ANT/STS, History & Philosophy of Science, Epistemology —
all of which resolve, so no faded chips. Regenerated data.js: 204 topics,
844 edges, zero parser warnings; tests 20/20.

- Docs: README counts, ARCHITECTURE thinker total (794 -> 801 people), PROMPT,
  memory.
- New LEARNINGS.md: the CSV has a CRLF header but LF records, and parse.mjs
  strips \r, so a header-split that keeps the trailing \r misaligns an
  appended row and still emits a warning-free data.js.
```


## Follow-up: reciprocal links (same session)

> Reciprocal links should always be updated when required.

Standing curation rule, not a one-off. Applied it to the 10 rows Critical
Theory links out to.

### Why it wasn't automatic
Nothing in the pipeline infers reciprocity: the detail panel renders *Outgoing*
and *Incoming* as separate lists, and the graph's `labelMinK` hub ranking is
**in-degree**-based. So a topic with 10 out / 0 in can never rank as a hub no
matter how central it is conceptually.

Context worth keeping: the dataset was only **19% reciprocated** before this
(160 of 844 directed edges). One-way links were the norm — historical curation
debt rather than deliberate asymmetry. Now 21.1% (180/854).

### How the edit was made (repeatable)
Surgical, not a full re-serialization: for each target row, compute its exact
serialized `connects_to_raw,connects_to_ids` pair, **assert it occurs exactly
once** in the file text, and swap in the extended version. Re-serializing all
204 rows would normalize quoting and line endings everywhere and bury the real
change.

Defensive step that mattered conceptually even though it was a no-op here: pad
`connects_to_ids` with empty slots up to `connects_to_raw`'s length **before**
appending. All 10 targets happened to be aligned, but on a row with trailing
unresolved slots the new id would otherwise pair with an earlier raw label and
silently mislabel the edge.

Also hit a triviality worth noting: a scratchpad script importing `./parse.mjs`
resolves relative to the *script*, not the cwd — needed the absolute project
path.

### Verification
- Diffed against `git show HEAD:research-topics.csv`: exactly **10 rows**
  changed, **only** in `connects_to_raw` / `connects_to_ids`, every
  pre-existing slot pairing byte-identical, back-link appended last in both
  columns. No other field on any row moved.
- `node parse.mjs` → 204 topics, **854 edges** (844 + 10), 263 unresolved
  (unchanged), **zero warnings**. Tests **20/20**.
- In-browser: panel reads **`in 10 · out 10`** with both *Outgoing — connects
  to (10)* and *Incoming — referenced by (10)* populated; all 10 out/in pairs
  mutual. Spot-checked two modified rows (Foucault `out 5→6`, Epistemology
  `out 5→6`) — new chip renders last and correctly labelled. *History &
  Philosophy of Science* card ticked 15 → 16 links. No console errors.

### Docs touched
README (edges 844→854; new paragraph on directed edges + the back-link rule +
the padding trap), ARCHITECTURE (reciprocity is not derived at runtime;
curation rule; the ~19% historical baseline), LEARNINGS (new "Edges are
directed — add the back-link yourself" section), PROMPT, this memory.

### Left alone deliberately
The ~674 other one-way edges predating this change. Retro-fitting them would
rewrite most of the 204 rows and is a judgement call per edge (some asymmetry
is correct — a broad field referencing a narrow one isn't always mutual).
Offered as a separate pass.

Also noticed but **not** changed: README line 116 says "Roughly 30% of raw
connection strings have no resolved target"; the real figure is ~24%
(263 unresolved of 1117 total slots). Pre-existing inaccuracy, unrelated to
this change — flagged to the user rather than silently edited.

### Suggested commit message (combined — covers both parts of this session)
```
data: add Critical Theory topic with reciprocal connections

The dataset had no Critical Theory entry despite carrying Foucault, ANT/STS,
and Media Theory - the discipline those topics implicitly lean on.

Add v9-critical-theory-and-the-frankfurt-school as id_code A7 in the existing
v9 "Group A - Disciplines & Fields" cohort, under discipline 5 (Culture,
History & Worldview) with likely_phase 5. Filed into v9 rather than a new v10
tag: a version tag is a UI facet (accent colour, legend chip, filter checkbox,
stats entry) and is not worth minting for a single row. The row follows the v9
Group A dialect - tier/phase/phase_rationale/primary_source empty, placement
via likely_phase.

Wire 10 connections (Media Theory, Foucault, Political Philosophy, Semiotics,
Aesthetics, Attention Economy, Sociology, ANT/STS, History & Philosophy of
Science, Epistemology), all of which resolve, and add the matching back-link
on each of those 10 rows so the topic reads in 10 / out 10. Edges are directed
and reciprocity is never inferred at runtime - the panel splits Outgoing from
Incoming and the graph's hub ranking is in-degree-based - so back-links have
to be curated. Document that as a standing rule in README, ARCHITECTURE and
LEARNINGS.

Regenerated data.js: 204 topics, 854 edges, zero parser warnings; tests 20/20.

- Docs: README counts and connection guidance, ARCHITECTURE (thinker total
  794 -> 801 people, reciprocity rule), PROMPT, memory.
- New LEARNINGS.md: the CSV has a CRLF header but LF records and parse.mjs
  strips \r, so a header-split that keeps the trailing \r misaligns an
  appended row and still emits a warning-free data.js; plus the
  connects_to_raw/connects_to_ids padding trap when appending back-links.
```
